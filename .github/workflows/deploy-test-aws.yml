name: Deploy to AWS

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: npm install
    - run: npm i -g claudia
    - name: Pack lambda
      run: claudia pack --no-optional-dependencies --output deploy/terraform/cms-api.zip
    - uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.3.6
        cli_config_credentials_token: ${{ github.event.client_payload.TERRAFORM_CLOUD_TOKEN }}
    - name: Terraform Apply
      run: terraform init && terraform apply -auto-approve
      working-directory: deploy/terraform
      env:
        TF_VAR_db_connection_string: ${{ github.event.client_payload.MONGO_CONNECTION_STRING }}
        TF_VAR_jwt_secret: ${{ github.event.client_payload.JWT_SECRET }}
        TF_VAR_stage: ${{ github.event.client_payload.STAGE }}
